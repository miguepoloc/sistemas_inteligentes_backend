"""This module contains the configuration of the ripener app."""

from django.apps import AppConfig


class RipenerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'ripener'
